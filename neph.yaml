main:
  depends_on:
    - man
    - binary

binary:
  command:
    shards build --release
  src:
    - src/**/*.cr

test:
  command:
    crystal spec

man:
  command:
    go-md2man -in neph.1.md -out neph.1
